{
	"name": "hello_world",
	"version": "1.0.0",
	"description": "hello world sample for NodeJS",
	"main": "app.js",
	"repository": "https://github.com/awslabs/aws-sam-cli/tree/develop/samcli/local/init/templates/cookiecutter-aws-sam-hello-nodejs",
	"author": "SAM CLI",
	"license": "MIT",
	"scripts": {
		"build": "esbuild src/**/*.ts --bundle --minify --platform=node --outdir=dist",
		"dev": "esbuild src/**/*.ts --bundle --platform=node --outdir=dist --watch"
	},
	"devDependencies": {
		"@types/aws-lambda": "^8.10.92",
		"@types/jsonwebtoken": "^8.5.9",
		"esbuild": "^0.15.12",
		"eslint": "^8.8.0",
		"typescript": "^4.5.5"
	},
	"dependencies": {
		"jsonwebtoken": "^8.5.1"
	}
}
